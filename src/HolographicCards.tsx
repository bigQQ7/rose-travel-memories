import React, { useState } from 'react';
import './holographic-cards.css';
const messages=['谢谢你把第一次旅行，变成我们故事的开场。','和你一起，普通的街道也值得慢慢走。','照片会褪色，但我记得你笑起来的样子。','愿我们去看更多海，也把每一次日落留给彼此。','谢谢你在我身边，把狼狈也变成了可爱的回忆。','下一次出发，还是想坐在你旁边。','我们走过的路，最后都变成了心里的风景。','希望以后每一个冬天，都有人陪你看雪。','这封信还没写完，下一页继续和你一起。'];
export default function HolographicCards(){const [flipped,setFlipped]=useState(messages.map(()=>false));const track=(e:React.PointerEvent<HTMLButtonElement>)=>{const r=e.currentTarget.getBoundingClientRect();e.currentTarget.style.setProperty('--mx',`${((e.clientX-r.left)/r.width)*100}%`);e.currentTarget.style.setProperty('--my',`${((e.clientY-r.top)/r.height)*100}%`)};return <section className="holo-cards" aria-labelledby="holo-cards-title"><p className="holo-kicker">A LITTLE SOMETHING</p><h2 id="holo-cards-title">翻开一张，看看我想说的话</h2><p className="holo-hint">点击卡牌翻面，移动鼠标可以看到彩色反光。</p><div className="holo-grid">{messages.map((m,i)=><button key={i} className={`holo-card ${flipped[i]?'is-flipped':''}`} onClick={()=>setFlipped(p=>p.map((v,j)=>j===i?!v:v))} onPointerMove={track} aria-pressed={flipped[i]} aria-label={`${flipped[i]?"收起":"翻开"}第 ${i+1} 张卡牌`}><span className="holo-card-inner"><span className="holo-face holo-back"><img className="card-art" src="./assets/pink-card-back.png" alt="" draggable={false}/><span className="holo-shine"/></span><span className="holo-face holo-front"><span className="front-mark">✦</span><span className="front-number">{String(i+1).padStart(2,'0')} / 09</span><span className="front-message">{m}</span><span className="front-sign">写给一直在我身边的你</span><span className="front-mark bottom">✦</span></span></span></button>)}</div></section>}
